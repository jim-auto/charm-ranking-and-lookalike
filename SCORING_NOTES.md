# SCORING_NOTES

## 現在のスコアリング

現在の顔スコアは次の 5 指標だけを使っています。

- `golden_ratio`: 35%
- `eyes`: 15%
- `nose`: 15%
- `mouth`: 15%
- `contour`: 20%

現在の実装は [scripts/process_faces.py](./scripts/process_faces.py) と [web/src/lib/faceScoring.ts](./web/src/lib/faceScoring.ts) で揃えています。

## 偏差値表示

ランキング画面の表示値は raw score ではなく偏差値です。

```txt
偏差値 = 50 + 10 * (score - mean) / stdev
```

`mean` と `stdev` は、その時点の全 celebrity データから計算します。  
したがって raw score が同じでも、外れ値を削除すると偏差値表示は少し動きます。

## 過去に試した重み変更

### 2026-03-14 より前の旧バックエンド重み

`scripts/process_faces.py` では長く次の重みを使っていました。

- `symmetry`: 20%
- `golden_ratio`: 25%
- `eyes`: 15%
- `nose`: 10%
- `mouth`: 10%
- `contour`: 10%
- `skin`: 10%

この構成は `symmetry` と `skin` の影響が大きく、写真条件に強く引っ張られやすい状態でした。

### 2026-03-14 03:36 JST (`823b3fd`)

- `symmetry`: 10% → 5%
- `nose`: 15% → 20%

意図:

- 眼鏡や照明で symmetry が過大評価されるバイアスを弱める

結果:

- コミットメッセージ上では `大森元貴: 9位 → 12位`
- 眼鏡やスタジオ写真に引っ張られるケースは減った

### 2026-03-14 10:38 JST (`a6aff0f`)

- `symmetry`: 5% → 0%
- `golden_ratio`: 30% → 35%
- `nose`: 20% → 15%
- `contour`: 15% → 20%

意図:

- 最も安定している構造指標に寄せる
- 写真依存の強い symmetry を完全に外す

結果:

- コミットメッセージ上では `大森元貴` の `symmetry=97` に引っ張られた順位上振れを解消
- 構造寄りの指標になり、理論上は再現性が上がった

### 2026-04-08 09:58 JST (`23d916a`)

口スコアの正規化を `nose_width` 基準から `face_width` 基準に変更しました。

変更前:

- `mouth_width / nose_width`
- ideal: `1.5`

変更後:

- `mouth_width / face_width`
- ideal: `0.38`

結果:

- 力士や顔幅の大きい人が「口の比率だけ偶然きれいに見える」問題を抑制
- 口スコアが顔サイズに対して素直な比較になった

### 2026-04-08 11:47 JST (`31fed58`)

Python のデータ生成側をフロントエンドと同じ重みに統一しました。

結果:

- 診断画面とランキングデータの整合性は改善
- 一方で、旧重みで見えにくかった外れ値が上位に浮きやすくなった
- 今回の外れ値削除は、その副作用に対するデータクリーニング

## symmetry と skin を除外した理由

### symmetry

- 角度、照明、メイク、表情、眼鏡、輪郭の検出誤差に強く依存する
- 顔そのものというより「写真の撮れ方」を評価しやすい
- 異常に高い symmetry 値 1 つで順位が跳ねることがあった

### skin

- 現在は実質的に固定値 `75`
- 個人差ではなく定数に近いので、重みに入れてもランキング情報量が増えない
- 光源やレタッチの影響を強く受けるので、写真依存の再導入にもなりやすい

## 力士・プロレスラーが高スコアになりやすい理由

- 顔幅や口幅が大きく、旧式の比率評価で有利になりやすかった
- 輪郭が強く出る公式写真や宣材写真が多く、`contour` や比率系で上振れしやすい
- 正面・高解像度・無表情の写真が多く、ランドマーク検出が安定しやすい
- 1 枚勝負だと、その人の普段の印象ではなく「たまたま都合のいい公式写真」が採用される

## Bing 画像検索のリスク

- 同姓同名の別人が混ざる
- グループ写真や記事サムネから別人物が拾われる
- 若い頃の写真、加工写真、舞台メイク写真が混ざる
- タレント本人ではなく似た顔のモデルや一般人画像が混入する

今回の `おぎやはぎ小木` のようなケースは、Bing 由来の取り込みミスを常に疑うべきです。

## 今後の改善案

- 1 人 1 枚ではなく、複数写真の中央値でスコアを出す
- 画像ごとに `source_url`, `source_type`, `collected_by` を保存して追跡可能にする
- Bing 取り込み画像は上位候補だけでも手動確認フローを入れる
- 力士・プロレスラー・舞台メイク写真など、バイアスが強いカテゴリに review フラグを持たせる
- ランキング生成後に `top N` を自動監査し、文化人・芸人・力士の異常上振れを検知する
- 同一人物の別名ディレクトリをマージし、重複データ混入を防ぐ

## 2026-04-08 外れ値整理と gender 補完

- `scripts/input_images` から `小林陵侑` `田臥勇太` `松田丈志` `福留孝介` `亀田興毅` を削除。さらに top20 に入っていた `田中みな実` `加藤綾子` も除外し、上位の `athlete` / `announcer` バイアスを軽減した
- `scripts/merge_metadata.py` を修正し、既存メタデータ適用後でも `category.txt` / `gender.txt` を読むようにした。これで既存人物でも `gender.txt` の修正が反映される
- `gender` 推定を追加し、`announcer` は個別補正、`athlete` / `comedian` は女性例外つき fallback 推定を入れた。再生成後の `gender=unknown` は `73 -> 0`
- cleanup 後の top20 から `田中みな実` `加藤綾子` `小林陵侑` は消え、`橋本環奈` は `12位 / 偏差値66.8` から `9位 / 偏差値67.3` に上がった。`浜辺美波` は `28位 / 偏差値62.7`
- 一方で `松岡茉優` は依然 `1位 / 偏差値77.3` で、`mouth=92` が強く効いている。画像差分か mouth 指標の外れ値耐性不足を疑う
- 男性上位はまだ `athlete` が強く、`横浜流星` は `47位 / 偏差値61.5`、`松坂桃李` は `122位 / 偏差値57.6`。データ cleanup と gender 補完だけでは理想分布までは届かず、次段階では画像差し替えか scoring 重み調整が必要
